[package]
name = "vrt-yolo"
version.workspace = true
edition.workspace = true
repository.workspace = true
license.workspace = true
description = "YOLO11/v8 detection for vision-rt — CPU letterbox, decode, NMS"

[lib]
name = "vrt_yolo"

[dependencies]
vision-rt      = { path = "../vrt" }
thiserror      = "2.0"
cudarc         = { version = "0.17", features = ["cuda-12060"] }
