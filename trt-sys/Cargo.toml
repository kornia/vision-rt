[package]
name = "trt-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Auto-generated FFI bindings to TensorRT 10.x via autocxx"
links = "nvinfer"

[lints.rust]
unsafe_code = "allow"

[build-dependencies]
autocxx-build = "0.30"
cc = "1"
bindgen = "0.70"   # still used for the btrt_* C bridge and logger shim

[dependencies]
autocxx = "0.30"
cxx = "1"
