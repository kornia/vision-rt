[workspace]
resolver = "2"
members = [
    "trt-sys",
    "trt",
    # trt-detector-node is a separate optional bubbaloop node — not part of the core library
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
authors = ["Edgar Riba <edgar.riba@gmail.com>"]
